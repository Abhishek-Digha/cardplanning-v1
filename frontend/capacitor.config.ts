import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ak47.storypointing',
  appName: 'Story Pointing Agile',
  webDir: 'build'
};

export default config;
